#!/bin/bash

set -o errexit
set -o pipefail

DIR="$(dirname "$0")"
. "${DIR}/config"

go mod tidy
go mod vendor

dapr run go run *.go \
    --app-id $APP_NAME \
    --app-port 8080 \
    --protocol http \
    --components-path ./components \
    --port 3500
